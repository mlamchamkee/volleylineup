# App
PORT=3000
ROOT_URL=http://localhost:3000
PG_URI=
KEY1=
KEY2=
HASH_KEY=

CLIENT_ID_GOOGLE=
CLIENT_SECRET_GOOGLE=
CLIENT_ID_FACEBOOK=
CLIENT_SECRET_FACEBOOK=

# Deploy — Docker / ECR (required for npm run deploy)
ECR_REPOSITORY_URI=
AWS_REGION=us-east-1

# Deploy — optional ECS rollout after image push
ECS_CLUSTER=
ECS_SERVICE=

# Deploy — Lambda (required for npm run deploy -- --lambda)
LAMBDA_GET_LINEUP=
LAMBDA_POST_LINEUP=

# Deploy — S3 static site (required for npm run deploy:s3)
AWS_BUCKET=
